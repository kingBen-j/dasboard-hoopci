# URL du backend HoopCI (sans slash final) — le dashboard est hébergé ailleurs
# et communique uniquement par API. En production : https://api.hoopci.ci par exemple.
VITE_API_URL=http://localhost:8000
